<Window x:Class="Diplom_F2_.AddTaskpanel.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:Diplom_F2_.AddTaskpanel"
        mc:Ignorable="d"
        Title="Window1" Height="550" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
       <StackPanel Margin="10" VerticalAlignment="Top">

            <TextBlock Text="Заголовок:" />
            <TextBox x:Name="TitleTextBox" MaxLength="150" Height="20"/>
            <TextBlock Text="Максимум 150 символов" 
                Foreground="Gray" 
                FontSize="10" 
                Margin="0,2,0,0" />

            <TextBlock Text="Сообщение:" Margin="0,10,0,0" />
            <TextBox x:Name="MessageTextBox" Height="80" AcceptsReturn="True" TextWrapping="Wrap"/>
            <TextBlock Text="Максимум 500 символов" 
                Foreground="Gray" 
                FontSize="10" 
                Margin="0,2,0,0" />

            <TextBlock Text="Телефон:" Margin="0,10,0,0" />
            <TextBox x:Name="PhoneTextBox" Height="20" MaxLength="25" TextChanged="PhoneTextBox_TextChanged" PreviewTextInput="PhoneTextBox_PreviewTextInput" PreviewLostKeyboardFocus="PhoneTextBox_PreviewLostKeyboardFocus" />
            <TextBlock Text="Введите номер телефона в формате +7XXXXXXXXXX" 
               Foreground="Gray" 
               FontSize="10" 
               Margin="0,2,0,0" />

            <TextBlock Text="Кабинет:" Margin="0,10,0,0" />
            <TextBox x:Name="CabinetTextBox" MaxLength="5" Height="20"/>

            <TextBlock Text="Адрес:" Margin="0,10,0,0" />
            <ComboBox x:Name="AddressComboBox" DisplayMemberPath="AddressO" SelectedValuePath="Id_locate_order" />
            <TextBlock Text="Если тут нет вашего адреса напишете в сообщение" 
                Foreground="Gray" 
                FontSize="10" 
                Margin="0,2,0,0" />


            <TextBlock Text="Департамент:" Margin="0,10,0,0" Visibility="Collapsed"/>
            <ComboBox x:Name="DepartmentComboBox" DisplayMemberPath="Name_departament" SelectedValuePath="Id_departament" Visibility="Collapsed" />

            <TextBlock Text="Статус:" Margin="0,10,0,0" Visibility="Collapsed" />
            <ComboBox x:Name="StatusComboBox" DisplayMemberPath="Name_status" SelectedValuePath="Id_status" Visibility="Collapsed" />
            
            <TextBlock Text="Дата создания:" Margin="0,10,0,0" />
            <DatePicker x:Name="OrderDatePicker" SelectedDate="{x:Static sys:DateTime.Now}" Loaded="OrderDatePicker_Loaded" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0" Width="auto">
                <Button Content="Выйти" Click="ExitButton_Click" Width="150" Height="40" Margin="0,0,450,0" HorizontalAlignment="Left"/>
                <Button Content="Сохранить" Margin="0,0,0,0" Click="SaveButton_Click" Width="150" Height="40" HorizontalAlignment="Center" />
            </StackPanel>
       </StackPanel>
    </Grid>
</Window>
